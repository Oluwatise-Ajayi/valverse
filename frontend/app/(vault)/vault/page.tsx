'use client';

import MediaVault from '@/components/vault/MediaVault';
import AppShell from '@/components/layout/AppShell';

export default function VaultPage() {
  return (
    <AppShell>
      <MediaVault />
    </AppShell>
  );
}
